# Save exact versions (prevents silent upgrades to malicious patch/minor versions)
save-exact=true

# Fail npm install if high or critical vulnerabilities are found
audit-level=high

# Enforce package-lock integrity on every install
package-lock=true
